# modules/features/server/syncthing/home.nix
# Syncthing file synchronization (home-manager)

{ config, pkgs, ... }:

{
  # Syncthing is configured system-wide
  # User interacts through web GUI or local sync
  # No home-manager specific configuration needed
}
