# modules/features/stylix/home.nix
# Home-manager stylix configuration

{ config, pkgs, ... }:

{
  # Stylix home configuration is handled by the NixOS module
  # This file exists for future home-specific theming
}
