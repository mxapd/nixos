# modules/features/base/home.nix
# Home-manager base configuration

{ config, pkgs, ... }:

{
  # Base home configuration that applies to all users
  # (empty for now - users define their own base)
}
