# modules/features/audio/home.nix
# Home-manager audio configuration

{ config, pkgs, ... }:

{
  # Audio is handled at system level via PipeWire
  # Home-manager can add audio-related user packages if needed
}
