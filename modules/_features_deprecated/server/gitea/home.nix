# modules/features/server/gitea/home.nix
# Gitea Git server (home-manager - user-level config)

{ config, pkgs, ... }:

{
  # Gitea is entirely system-level
  # Users interact with it through git or web browser
  # No home-manager specific configuration needed
}
