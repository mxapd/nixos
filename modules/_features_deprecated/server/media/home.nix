# modules/features/server/media/home.nix
# Media server (home-manager)

{ config, pkgs, ... }:

{
  # Media servers are system-level
  # Users interact through web browsers or client apps
  # No home-manager specific configuration needed
}
